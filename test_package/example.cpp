#include <logical_geometry/ESSGeometry.h>
#include <iostream>

int main() {
  ESSGeometry g;
  // std::cout << g.nx() << ", " << g.ny() << ", " << g.nz() << ", " << g.np()
  //           << '\n';
  return 0;
}
